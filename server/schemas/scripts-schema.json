{
  "addNewScript": {
    "id": "AddNewScript",
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "pattern": "^[a-zA-Z0-9][a-zA-Z0-9-_\\.]+$"
      },
      "description": {"type": "string"},
      "code": {"type": "string"}
    },
    "required": ["name", "description", "code"],
    "minProperties": 3,
    "maxProperties": 3
  },
  "updateScript": {
    "id": "UpdateScript",
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "pattern": "^[a-zA-Z0-9][a-zA-Z0-9-_\\.]+$"
      },
      "description": {"type": "string"},
      "code": {"type": "string"}
    },
    "required": ["name", "description", "code"],
    "minProperties": 3,
    "maxProperties": 3
  }
}
